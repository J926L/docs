mcpServers:
  # Sequential Thinking: 深度推理/链式思考增强
  # sequential-thinking:
  #   command: npx
  #   args:
  #     - -y
  #     - "@modelcontextprotocol/server-sequential-thinking"
  Context7:
    command: npx
    args:
      - -y
      - "@upstash/context7-mcp@latest"
  supermemory:
    command: npx
    args:
      - -y
      - "@supermemory/mcp-server"
    env:
      SUPERMEMORY_API_KEY: "${SUPERMEMORY_API_KEY}"
  Tavily Search:
    command: uvx
    args:
      - mcp-server-tavily
    env:
      TAVILY_API_KEY: "${TAVILY_API_KEY}"
  github:
    command: npx
    args:
      - -y
      - "@modelcontextprotocol/server-github"
    env:
      GITHUB_PERSONAL_ACCESS_TOKEN: "${GITHUB_TOKEN}"
  postgres:
    command: npx
    args:
      - -y
      - "@modelcontextprotocol/server-postgres"
      - "${POSTGRES_URL}"
  redis:
    command: npx
    args:
      - -y
      - "@modelcontextprotocol/server-redis"
      - "${REDIS_URL}"
  # Chrome DevTools: 调试前端/Tauri (需先启动 Chrome --remote-debugging-port=9222)
  # chrome-devtools:
  #   command: npx
  #   args:
  #     - "chrome-devtools-mcp@latest"
  #     - "--browser-url=http://127.0.0.1:9222"
  #     - "-y"
  # MarkItDown: 多格式转 Markdown (Word, PDF, PPT, etc.)
  # markitdown:
  #   command: npx
  #   args:
  #     - -y
  #     - "@microsoft/markitdown-mcp"
  # Serena: AI 编码增强
  # serena:
  #   command: npx
  #   args:
  #     - -y
  #     - @oraios/serena
